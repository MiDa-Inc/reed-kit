import SwiftUI

/// Default "Charcoal · Soft Tonal" palette for the bundled UI. Override the
/// button tint via `HoldToTalkButton(engine:tint:)`.
public enum ReedTheme {
    public static let charcoal = Color(red: 58 / 255, green: 58 / 255, blue: 60 / 255)
    public static let micBG    = Color(red: 230 / 255, green: 230 / 255, blue: 235 / 255)
    public static let ring     = Color(red: 184 / 255, green: 184 / 255, blue: 190 / 255)
}
